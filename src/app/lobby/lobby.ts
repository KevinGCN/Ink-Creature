import { Component } from '@angular/core';
import { RouterLink } from "@angular/router";

@Component({
  selector: 'app-lobby',
  imports: [RouterLink],
  standalone: true,
  imports: [Loggin],
  templateUrl: './lobby.html',
  styleUrls: ['./lobby.css']
})

export class Lobby {

}